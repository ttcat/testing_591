document.querySelectorAll('.compare-box').forEach(box=>{const slider=box.querySelector('.compare-slider');const after=box.querySelector('.after-wrap');if(!slider||!after)return;const update=()=>after.style.clipPath=`inset(0 0 0 ${slider.value}%)`;slider.addEventListener('input',update);update();});

document.querySelectorAll('.filter').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.compare-row').forEach(row=>row.hidden=!(f==='all'||row.dataset.category===f));}));

const lightbox=document.querySelector('.lightbox');const lightboxImg=lightbox?.querySelector('img');document.querySelectorAll('.tour-photo').forEach(btn=>btn.addEventListener('click',()=>{if(!lightbox||!lightboxImg)return;lightboxImg.src=btn.querySelector('img').src;lightbox.hidden=false;document.body.style.overflow='hidden';}));const closeLightbox=()=>{if(!lightbox)return;lightbox.hidden=true;document.body.style.overflow='';};lightbox?.querySelector('.lightbox-close')?.addEventListener('click',closeLightbox);lightbox?.addEventListener('click',e=>{if(e.target===lightbox)closeLightbox();});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeLightbox();});

const booking=document.querySelector('#bookingForm');const bookingStatus=document.querySelector('#bookingStatus');booking?.addEventListener('submit',e=>{e.preventDefault();bookingStatus.textContent='資料格式已完成；下一步可串接 Google Form / Calendar 讓預約真的送出。';});